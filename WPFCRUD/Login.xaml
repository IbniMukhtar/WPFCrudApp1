<Page x:Class="WPFCRUD.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFCRUD"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <!-- Left half of the screen for dynamic images -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#FF6699FF">
            <Image x:Name="DynamicImage" Stretch="UniformToFill"/>
        </Grid>

        <!-- Right half of the screen for login content -->
        <Grid Grid.Column="1" Background="#FFC0C0C0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBox x:Name="UsernameTextBox" Width="200" Margin="0,0,0,10" />
                <PasswordBox x:Name="PasswordBox" Width="200" Margin="0,0,0,10" />
                <Button Content="Login" Width="100" Click="LoginButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
